/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  outputFileTracingRoot: "D:/El Drago/problem_08",
  experimental: {
    externalDir: true,
  },
};
export default nextConfig;
